name: feedback
description: >
  Vendored fork of feedback 3.2.0 (FlutterAdgents) — barre d’outils : bouton pour agrandir la bottom sheet.
version: 3.2.0+flutteradgents.2
homepage: https://uekoetter.dev/
repository: https://github.com/ueman/feedback
issue_tracker: https://github.com/ueman/feedback/issues

funding:
  - https://github.com/sponsors/ueman

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter